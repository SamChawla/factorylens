# Secrets — never commit the real thing
.env
.env.*
!.env.example

# Internal working notes (scope, standards, decision log, build plan).
# Kept locally; not part of the shipped project.
.cursor/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
coverage.xml
htmlcov/
.ruff_cache/
.venv/
venv/

# uv
# uv.lock IS committed on purpose (reproducible demo-day builds)

# Data / artifacts
*.parquet
data/*.csv
!data/.gitkeep

# Demo capture output (screenshots + video) — regenerable via scripts/capture_demo.py
scripts/output/

# Foundry deployment output (generated by `foundryctl cast`)
pours/

# OS / editor
.DS_Store
Thumbs.db
.idea/
